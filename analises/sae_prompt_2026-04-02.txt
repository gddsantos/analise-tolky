<generalInstructions> --- <instructions> <context> You will read a conversation between a User and an AI assistant. Your task is to decide which tag names from **<allInstructions>** apply to the User’s latest message. Consider both the latest message and the broader conversation context when deciding. </context> <instructions> • Examine every tag definition in **<allInstructions>**. • A tag qualifies when the User’s message, or its clear contextual continuation within the conversation, satisfies at least one of the tag’s listed conditions. • If the message is unrelated to any defined tag, return an empty list. • Use literal evidence when possible, but allow minimal contextual inference for clear continuations. • If system messages indicate that certain tags were already selected earlier, do not repeat them. • Consult the per-tag **consequences** defined in **<allInstructions>** only to decide; **return ONLY the selected tag(s)** (do not return agents). </instructions> </instructions> --- <logged> Este usuário está logado. </logged> --- Consider that today is <actual_date> 2 de abril de 2026 às 12:06:31 (Brazil) </actual_date> </generalInstructions>